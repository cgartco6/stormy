class NavigationAgent:
    description = "Handles navigation, maps, and routing."
    def process(self, instruction, context):
        # Use navigation.py
        return "Navigating to destination."

class MusicAgent:
    description = "Plays music, manages playlists, radio."
    def process(self, instruction, context):
        # Use music.py
        return "Playing your favorite tunes."

class ProductivityAgent:
    description = "Manages notes, reminders, and tasks."
    def process(self, instruction, context):
        # Use notes
        return "Note added."
